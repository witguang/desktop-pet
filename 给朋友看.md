# 桌面宠物 — 给朋友的安装说明

你**不需要**装 Python。

## 一步搞定

1. 打开 [Releases](https://github.com/witguang/desktop-pet/releases)
2. 下载 **`DesktopPet-v*-windows.zip`**（或直接拿到 **`DesktopPet.exe`**）
3. 解压后**只双击 `DesktopPet.exe`**
4. 首次会弹出设置（安装位置、备忘录、桌面快捷方式、是否打开桌宠）  
   - 界面会**提醒建议放在 D 盘**，但路径需自己选，不会自动填文件夹  
   - 默认勾选快捷方式与「完成后打开桌宠」（可取消）

安装目录里正常只有 **一个 `DesktopPet.exe`**（用一阵后会有 `data_store` 存你的设置，可忽略）。

## 注意

| 情况 | 处理 |
|------|------|
| Windows「未知发布者」 | 「更多信息」→「仍要运行」（开源自签包常见） |
| 想开机自启 | 右键桌宠 → 主设置 → 开机自启 |
| 卸载 | 退出桌宠 → 删除安装文件夹 → 删除桌面/启动项快捷方式；备忘录目录可保留 |
| 数据在哪 | 安装目录下 `data_store/`（设置与任务日志）；备忘录在你选的文件夹 |

## 操作

| 做什么 | 怎么做 |
|--------|--------|
| 移动 | 左键拖 |
| 控制面板 | 右键，或 `Ctrl+Shift+P` |
| 退出 | 控制面板 / 主设置 → 退出桌宠 |
