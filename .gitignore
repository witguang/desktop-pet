__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.env
.venv/
venv/
.env.*
*.log
.DS_Store
Thumbs.db

# 用户数据与本地历史（更新器也不会覆盖 data_store）
data_store/
!data_store/.gitkeep

# 本地参考素材 / 生成缓存
ref/
assets/characters/

# IDE
.idea/
.vscode/
*.swp

# 打包产物（体积大，不进仓库）
build/
dist/
*.egg-info/

# 维护者本地脚本（含 API 推送、一次性修复等，可能含账号/环境假设）
scripts/

# PyInstaller 自动生成的绝对路径 spec（保留仓库内可移植的 DesktopPet.spec / build_app.spec）
# 若本地再 pyi-makespec，勿提交带盘符的路径
