__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.env
.venv/
venv/
.env.*
*.log
.DS_Store
Thumbs.db

# 用户数据与本地历史（更新器也不会覆盖 data_store）
data_store/
!data_store/.gitkeep
# 更新自动重启临时脚本
data_store/_auto_restart.cmd
data_store/_auto_restart.sh

# 本地参考素材 / 生成缓存
ref/
assets/characters/

# IDE
.idea/
.vscode/
*.swp

# 打包产物（体积大，不进仓库）
build/
dist/
*.egg-info/
app.ico
packaging/app.ico

# 维护者本地脚本（含 API 推送、一次性修复等，可能含账号/环境假设）
scripts/

# packaging/ 内 DesktopPet.spec / DesktopPetSetup.spec / build_app.spec 为可移植手写版，需进仓库
# 勿提交 PyInstaller 自动生成、含本机盘符的 .spec
